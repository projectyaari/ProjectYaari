name: iOS Build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-ios:
    name: Build iOS App
    runs-on: macos-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Xcode
      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.0'   # Replace with the Xcode version your project needs

      # 3️⃣ Install dependencies (CocoaPods, if used)
      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods
          pod install --project-directory=.

      # 4️⃣ Build the app
      - name: Build iOS App
        run: |
          xcodebuild clean \
            -project ProjectYaari.xcodeproj \
            -scheme ProjectYaari \
            -sdk iphonesimulator \
            -configuration Debug \
            build
